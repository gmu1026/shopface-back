<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dreamsecurity.shopface.sample.SampleMapper">
    <select id="select" parameterType="long" resultType="com.dreamsecurity.shopface.sample.Sample">
        SELECT * FROM SAMPLE WHERE NO= #{no}
    </select>
</mapper>